import { type DataTypeWithProperties } from '../schema';

export async function getDataTypes(): Promise<DataTypeWithProperties[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all custom data types with their properties
    // from the database for display and selection.
    return Promise.resolve([]);
}